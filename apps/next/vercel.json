{
  "installCommand": "yarn set version 4 && yarn install --immutable",
  "buildCommand": "cd ../.. && yarn build && cd apps/next && next build",
  "outputDirectory": ".next",
  "framework": "nextjs"
}
