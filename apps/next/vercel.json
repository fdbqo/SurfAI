{
  "installCommand": "yarn set version 4.5.0 && yarn install --immutable",
  "buildCommand": "cd ../.. && yarn build && cd apps/next && next build",
  "outputDirectory": ".next",
  "framework": "nextjs"
}
